source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in flexible_filter.gemspec.
gemspec

gem "puma"
gem "pg"
gem "sprockets-rails"
gem "draper"
gem "kaminari"
gem "view_component"
gem "simple_form"
gem "ransack"

group :development, :test do
  # Start debugger with binding.b [https://github.com/ruby/debug]
  # gem "debug", ">= 1.0.0"
  gem "rspec-rails"
  gem "factory_bot_rails"
end

group :development do
end
